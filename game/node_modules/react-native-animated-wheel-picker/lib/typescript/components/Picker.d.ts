import { ViewProps, StyleProp, TextStyle, ViewStyle } from 'react-native';
export declare type PickerData = {
    title: string;
    value: any;
};
export declare type PickerProps = ViewProps & {
    itemHeight?: number;
    pickerData: PickerData[];
    initialIndex?: number;
    visible?: number;
    textStyle?: StyleProp<TextStyle>;
    maskedComponents?: JSX.Element | JSX.Element[];
    contentContainerStyle?: StyleProp<ViewStyle>;
    onSelected: (data: PickerData, index: number) => void;
};
declare const Picker: ({ itemHeight, pickerData, visible, textStyle, maskedComponents, contentContainerStyle, initialIndex, onSelected, ...props }: PickerProps) => JSX.Element;
export default Picker;
